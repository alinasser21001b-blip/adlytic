{
  "name": "@dawai/platform",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "npm run trace && npm run layers && npm run ux && npm run types && npm run test",
    "trace": "node tools/trace-check.mjs",
    "layers": "node tools/layer-check.mjs",
    "ux": "node tools/ux-check.mjs",
    "types": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
