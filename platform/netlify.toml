# SUPERSEDED — deliberately empty.
#
# This file briefly configured a SECOND Netlify site for the patient app. It no
# longer does: product asked that the EXISTING preview URL always show the
# latest build rather than a new address, so the configuration moved to the
# netlify.toml at the repository root, which the existing site already reads.
#
# It is kept as an empty file rather than removed so that a site whose base
# directory is set to `platform` does not silently fall back to a stale copy of
# the old settings. Everything that matters — base, publish, build command, the
# VITE_MOCK_API flag, the routing rewrites, the caching and the permissions
# policy — lives at the repository root. Edit it there.
