name = "adlytic"
type = "service"
main = "dist/src/api/serve.js"

build = { command = "npm run build" }

[env.production]
name = "adlytic"

# Netlify Functions — excluded from Cloudflare
# pharmacy/netlify/functions/* are for Netlify only
